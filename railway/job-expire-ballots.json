{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "dockerfilePath": "Dockerfile" },
  "deploy": {
    "cronSchedule": "*/10 * * * *",
    "restartPolicyType": "NEVER",
    "startCommand": "node --import ./scripts/register-path-aliases.mjs scripts/job-expire-ballots.ts --batch 500"
  }
}
