# Local image attribution

Player photos and Team logos must be copied into `public/images/players` or `public/images/teams`
only when the owner supplied them, permission was granted, or a compatible license is documented.
Every imported file must have one matching record in `attribution.json`. Runtime hotlinking and
automatic copying from provider pages are prohibited.

Run `pnpm assets:check` before committing asset changes.
